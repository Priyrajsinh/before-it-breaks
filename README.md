# Before It Breaks — Predictive Maintenance · Remaining Useful Life

NASA CMAPSS FD001 · PyTorch LSTM (sliding-window degradation) · SHAP GradientExplainer · FastAPI · Gradio · Streamlit.

> Project under construction (Day 0 of 9). Final README + live demos land on Day 8.

## Quick start (after Day 1 ships)
```bash
make install
make train
make evaluate
make serve     # http://localhost:8000/docs
```

See `CLAUDE.md` for project conventions, dataset schema, and the piecewise-linear RUL narrative this project is built around.
